# Minimal config — all infrastructure (bindings, KV, R2, vars, queue consumers)
# is managed by Terraform. This file only tells Wrangler how to build the worker.
# The worker name is overridden in CI via: wrangler deploy --name $WORKER_SCRIPT_NAME

name = "game-zip-processor"
main = "src/index.ts"
compatibility_date = "2024-01-01"
