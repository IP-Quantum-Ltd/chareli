name = "game-zip-processor" # Overridden at deploy time via WORKER_NAME in CI
main = "src/index.ts"
compatibility_date = "2026-02-24"
compatibility_flags = ["nodejs_compat"]

# Bindings are NOT defined here — they are managed exclusively by Terraform (worker.tf).
# This file is only used by wrangler for the --dry-run build step.
# Deployment uses the Cloudflare Workers REST API (curl) to update script content only,
# which preserves all existing Terraform-managed bindings.

